db:
  image: mongo:2.6
  volumes:
    - ./data/mongo:/data/db
  ports:
    - "27017"

web:
  build: .
  command: revel run licorne
  links:
    - db
  volumes:
    - .:/go/src/licorne
    - $SSH_AUTH_SOCK:/ssh-agent
  environment:
    - SSH_AUTH_SOCK=/ssh-agent
  ports:
    - "9000:9000"
